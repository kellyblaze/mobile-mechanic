# Repository operating agreement

Read `docs/product-build-agreement.md`, `docs/architecture.md`, `packages/contracts/openapi.yaml`, and `docs/integration-status.md` before changing this repository.

Codex owns `apps/api`, `apps/worker`, `packages/contracts`, `packages/domain`, `packages/database`, `packages/config`, `infra`, migrations, and the root toolchain. Claude owns `apps/web` and `packages/ui`. Generated contract files are not hand-edited. Contract changes require `docs/change-requests/CR-###.md` and a new contract version.
