# Claude-owned frontend

This directory is a minimal placeholder for Claude. Create the frontend here without modifying server-owned packages. Use `packages/contracts/generated/client.ts`, `packages/contracts/fixtures/index.json`, and `/api/v1` through one adapter/query layer.
