# Copy to .env and fill in with your Supabase project's anon (publishable) key and URL — the
# same values that live server-side as SUPABASE_URL/SUPABASE_ANON_KEY. Safe for client-side use
# by Supabase's own design (protected by RLS); never put the service-role key here.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Stripe publishable key (test mode) — the same value as the server's STRIPE_PUBLISHABLE_KEY.
# Safe for client-side use by Stripe's own design; never put the secret key here.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key

# Optional — defaults to http://127.0.0.1:3000/api/v1 if unset.
VITE_API_BASE_URL=http://127.0.0.1:3000/api/v1
