# Integration status

| Feature | Backend | Frontend | Real integration | Evidence / blocker |
|---|---|---|---|---|
| Health/session | Supabase JWT validation plus explicit development adapter | implemented: real sign-in screen and bearer-token adapter; `AdminProvisionMembership` (`/admin/memberships/new`) wraps `POST /admin/memberships` so an admin can grant a role by email without curl | integrated — live-verified all three response paths: unknown email returned 404 in the UI, non-admin actor denied (403, exercised server-side), known email granted a role and showed the real success message | No public self-signup. Provision accounts through the approved Supabase/admin process, then grant business membership through `/admin/memberships/new`. |
| Service catalog | implemented via in-memory adapter | implemented (Home) | integrated | verified via browser against real API; business catalog/prices not configured |
| Vehicle create/list | PostgreSQL repository when configured; fixture fallback only without DB/mock | implemented (My Garage: list + create form) | managed JWT and PostgreSQL paths verified | business-specific vehicle policy remains minimal |
| Service request submission | implemented | integrated (Tap Your Trouble submit) | integrated against development adapter | durable repository and production routing pending |
| Upload initiation metadata | implemented via safe local adapter | not wired | mocked/pending | private signed object storage pending |
| Quote acceptance | implemented with version conflict checks | implemented (Repair Room: real `GET /quotes/{id}` via `job.quoteId`, no more hardcoded quote-1/version 1 — CR-002 frontend cleanup done) | integrated | verified via browser + curl against real Postgres-backed API |
| Repair Room read | implemented, real Postgres persistence | implemented (status, version, allowedActions) | integrated | **Found live**: real `GET /jobs/{id}` response omits `allowedActions` entirely (not just empty) for the seeded job — `Job.allowedActions` changed to optional in `apiAdapter.ts` and `RepairRoom.tsx` defends with `?? []`; confirmed no crash against real data after the fix. Also found live: the old `job-1` fixture id 403s under real persistence — nav link and route default updated to the real seeded UUID (`packages/database/src/seed.ts`). |
| Repair Room messages/findings | durable PostgreSQL read/write routes | integrated (read thread, send message, findings empty/list states) | managed role matrix and durable DB path verified | realtime delivery and attachment storage pending |
| Tap Your Trouble intake | implemented (`POST /service-requests`, upload metadata) | integrated (`POST /service-requests`) | development adapter | real object storage upload still pending |
| Booking/availability | `GET /availability`, business-scoped/deduplicated `GET /mechanics`, `POST /booking-holds`, `POST /booking-holds/{id}/confirm`, and `POST /appointments/{id}/cancel` implemented | implemented: `Booking` picks a real mechanic, checks availability, holds a slot, confirms it, and — right after confirming — can cancel that same booking with an optional reason | booking flow and cancellation both live-verified: success, 403 non-participant, 404 already-cancelled, 409 within-two-hours cutoff, and admin bypassing the cutoff all confirmed against the real API | Customers/mechanics require at least two hours notice; admins may cancel future appointments; refunds remain admin-controlled. Cancellation is only reachable right after booking today — no API response ever exposes an existing appointment's id afterward (not `GET /jobs/{id}`, not the job list screens), so there's no cancel action yet on Repair Room, Admin Jobs, or My Jobs; see [CR-015](change-requests/CR-015.md). Rescheduling remains separate follow-up work. |
| Invoices | `GET /invoices` (customer) and `POST /admin/jobs/{id}/invoices` (admin) implemented | implemented: Repair Room's invoices section (filtered client-side to the job) plus new `AdminIssueInvoice` (`/admin/jobs/:id/invoice`) for admins to create one | fully live-verified: admin issued a real invoice through the UI for a real job, it appeared in the customer's Repair Room with a "Pay" link | openapi.yaml has no response schema for either operation — shapes confirmed by reading the repository code directly, not guessed. |
| Uploads | uploads remain metadata-only | not wired | mocked/pending | `POST /uploads` confirmed live via curl to return `status: "pending_local_adapter"` with no real upload URL — the Tap Your Trouble photo step still can't send real bytes. |
| Payments | Stripe payment intent/refund routes and signed webhook reconciliation implemented, including invoice linkage | implemented: `PayInvoice` links the payment to the real invoice and confirms it with Stripe's `PaymentElement` | fully live-verified: application-created invoice-linked PaymentIntent succeeded, signed Stripe listener deliveries returned HTTP 200, the invoice became `paid`, and refund reconciliation completed | PostgreSQL `bigint` amount normalization and frontend numeric serialization are now covered. Provider retry/monitoring hardening remains a deployment concern. |
| Admin/mechanic workflows | quote issuance, service-request discovery, job operations, admin/mechanic lists, transitions, completion, findings | implemented (`MechanicJobs`, `AdminJobs`, `AdminIssueQuote`, `AdminProvisionMembership`, and Repair Room controls) | integrated — admin picker returned 200 with customer/vehicle context; customer access returned 403; issuing a quote changed the request from `submitted` to `quoted` and removed it from the submitted picker | `JobTransition.status` remains a free-text contract field until a status policy is approved. |
| My Garage / Vehicle Passport | vehicle fields plus durable `/vehicles/{id}/history` | vehicle info and history consumer wired with real loading/error/empty/list states | verified | Fixed the history route to use the PostgreSQL vehicle ownership path for UUID-backed vehicles; customer flow returns history and mechanic access remains 403. |
| Get My Car Right (phased plans) | contract planned only | not started | mocked/pending | out of scope until scheduling/change-order contract exists |
| Admin monitoring | Admin-only `/admin/monitoring/*` proxy routes implemented; GlitchTip issue data requires server-side configuration | implemented against the real proxy by default, with an explicit dev-only "Use mock data" toggle per screen for local preview | fully live-verified: overview/webhooks/reconciliation/uptime return real database-backed data; issue list/detail correctly 503 `MONITORING_UNAVAILABLE` in this dev environment (no `GLITCHTIP_*` vars set) and the UI shows an honest "not configured" state, distinct from a generic error | **Found and fixed live**: the four Monitoring screens' query keys didn't include `actorKey`, so switching the dev-role selector didn't trigger a refetch — customer's cached 403 kept showing after switching to admin. Also found and fixed: a long unbroken string in the "GlitchTip not configured" message (env var names joined by `/`) had no wrap point and overflowed at 375px — `.pending-note` now has `overflow-wrap: anywhere`. `issues.open/critical/last24Hours` on the overview are hardcoded to 0 server-side regardless of real state — not yet wired to GlitchTip even when configured; the UI discloses this. Never expose GlitchTip API tokens to the browser — none appear in source, network payloads, or localStorage (all verified live). |

Business inputs still required: real business identity, service area/timezone, hours/staff, pricing/taxes/fees, deposit/cancellation rules, payment account, notification preferences, warranty terms, storage/auth providers, and bodywork partner details.

Verification scripts: `pnpm test:managed-auth` runs a disposable Supabase customer journey against a managed API; `pnpm test:managed-matrix` creates disposable customer/mechanic/admin users and checks role permissions. Start the API with `AUTH_MODE=managed` and run Stripe CLI with `stripe listen --forward-to http://127.0.0.1:3006/api/v1/webhooks/stripe` for signed test delivery.

**Local dev note (resolved):** `.env` now has `AUTH_MODE=development`, so the plain `X-Dev-User-Id`/`X-Dev-Role` headers `docs/frontend-handoff.md` documents work again for local frontend dev without any process-level override. Verified live via browser and curl on 2026-09-10.
